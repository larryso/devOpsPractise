parameters:
  - name: p1
    type: string
    default: hello
  - name: p2
    type: boolean
    default: true

steps:
  - task: Bash@2
    displayName: Initialization task
    inputs:
      targetType: 'inline'
      script: |
         echo start pipeline initialization....